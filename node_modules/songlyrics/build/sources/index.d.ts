export declare const useSpacingLyrics: (lyrics: string) => string;
export declare type TSource = {
    name: string;
    hostname: string;
    path: string;
    parse: (html: string) => Promise<string>;
};
declare const sources: TSource[];
export default sources;
